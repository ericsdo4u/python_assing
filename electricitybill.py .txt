unit = int(input('Enter a unit: '))
Amount = 0:
if unit <= 100:
	print('No charges', Amount)

elif unit > 100 and unit < 200
amount = (unit - 100) * 10:
	print(Amount)

elif unit > 200
	Amount = (100 * 10) + (unit - 100) * 20
	print(Amount)
	
